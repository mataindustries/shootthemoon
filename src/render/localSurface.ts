/**
 * Visual separation between the mean reference sphere and the close-range
 * terrain overlay. Canonical landing altitude remains unchanged.
 */
export const LOCAL_SURFACE_RENDER_OFFSET = 0.00042
